
import { getLabel } from 'test-app/helpers/get-label';
import { module, test } from 'qunit';

module('Unit | Helper | get label');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getLabel([42]);
  assert.ok(result);
});

