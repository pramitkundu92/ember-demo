
import { getLength } from 'test-app/helpers/get-length';
import { module, test } from 'qunit';

module('Unit | Helper | get length');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getLength([42]);
  assert.ok(result);
});

